{
  "name": "udodiri-lambda-functions",
  "version": "1.0.0",
  "description": "Lambda functions for Udodiri Social Club",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf node_modules dist"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-secrets-manager": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
